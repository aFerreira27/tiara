specVersion: "v1"
serviceId: "tiara"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "productInfo"
      cloudSql:
        instanceId: "tiarasql"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
